version: '3'

services:
  samba:
    tty: true
    network_mode: bridge
    volumes:
      - /smb-shares/media/shared/:/Shared
    ports:
      - "445:445"
    build:
      context: .
      dockerfile: Dockerfile
